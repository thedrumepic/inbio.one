# Замена приветственных тостов на настраиваемое модальное окно (Выполнено)

Задача выполнена. Удалены старые тосты, создана система глобальных настроек, реализовано модальное окно WelcomeModal и управление им в админ-панели.

## Отчет о выполненных действиях

### 1. Удаление дублирующихся тостов (Выполнено)
**Файлы:**
- `frontend/src/pages/Login.js` (Удалено)
- `frontend/src/pages/Register.js` (Удалено)
- `frontend/src/pages/Dashboard.js` (Удалено, заменено на модалку)
**Результат:** Тосты удалены.

### 2. Бэкенд: Глобальные настройки (Выполнено)
**Файлы:**
- `backend/server.py`
**Результат:** Добавлены эндпоинты `/api/admin/settings` (GET, PATCH) и `/api/settings/public` (GET). Модель `GlobalSettings`.

### 3. Фронтенд: Компонент WelcomeModal (Выполнено)
**Файлы:**
- `frontend/src/components/ui/WelcomeModal.js`
**Результат:** Создан компонент с текстом и таймером. Таймер блокирует кнопку закрытия на заданное время.

### 4. Фронтенд: Админ-панель "Функции сайта" (Выполнено)
**Файлы:**
- `frontend/src/pages/AdminDashboard.js`
**Результат:** Добавлена вкладка "Функции сайта" с настройкой длительности модального окна (слайдер 1-60 сек).

### 5. Интеграция (Выполнено)
**Файлы:**
- `frontend/src/pages/Dashboard.js`
**Результат:** Модалка показывается новым пользователям (проверка через localStorage `welcomeShown`). Время берется с сервера.

## Текст модального окна
> Добро пожаловать в мир InBio.One! 🎉
> Спасибо, что выбрали нас для развития своих проектов.
> Наша система устроена просто и эффективно:
> **Основная страница:** Ваша самая первая ссылка — это цифровой паспорт и лицо вашего бренда в соцсетях.
> **Brand-страницы:** Все последующие страницы созданы для ваших отдельных проектов: магазинов, музыкальных групп или мероприятий.
> С уважением, команда InBio.One


## Исправление системы ролей (RBAC) (Выполнено)
**Цель:** Убрать роль `admin`, оставить только `owner` (thedrumepic@gmail.com) и `user`.
**Действия:**
1. `backend/server.py`:
   - В `get_current_user_optional` добавлена принудительная установка роли `owner` для `thedrumepic@gmail.com` и `user` для остальных.
   - В `get_current_admin` убрана возможность входа по паролю (bypass) и добавлена проверка `role == "owner"`.
   - В `register` и `google_auth` роль `admin` больше не назначается.

## Primary Page Protection (Выполнено)
**Цель:** Первая созданная страница (при регистрации) считается основной (`is_main_page=True`) и не может быть удалена. Она всегда отображается первой в списке.
**Действия:**
1. `backend/server.py`:
   - `get_user_pages`: Автоматическая миграция (если нет главной, самая старая становится главной) и сортировка (главная первая).
   - `create_page`: Новые страницы получают `is_main_page=False` (если у пользователя уже есть страницы).
   - `register` / `google_auth`: Первая страница создается с `is_main_page=True`.
   - `delete_page`: Блокировка удаления для главной страницы.
2. `frontend/src/pages/Dashboard.js`:
   - Скрытие кнопки удаления для главной страницы.
